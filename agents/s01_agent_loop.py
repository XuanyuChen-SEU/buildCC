#!/usr/bin/env python3
# 框架：循环 —— 持续将真实工具结果回传给模型。
"""
s01_agent_loop.py - 智能体循环

这个文件演示了最小可用的编码智能体模式：

    用户消息
      -> 模型回复
      -> 如果发生 tool_use：执行工具
      -> 将 tool_result 写回 messages
      -> 继续循环

它有意保持循环结构精简，但仍然显式展示循环状态，
这样后续章节就可以在同一结构上逐步扩展。
"""

